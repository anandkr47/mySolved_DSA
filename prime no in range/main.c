#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j;
    int p,q;
    int c=0;
    printf("enter the upper range for prime number\n");
    scanf("%d",&p);
    printf("enter the lower range ");
    scanf("%d",&q);
    i=q;
    while(i<=p){
        j=1;

        if(a%b==0)
            c++;
        b++;
    }
         i++;

        if(c==2)
            printf("prime no are %d \n",i);
        else
            printf("there are no prime numbers");
    }

    return 0;
}
